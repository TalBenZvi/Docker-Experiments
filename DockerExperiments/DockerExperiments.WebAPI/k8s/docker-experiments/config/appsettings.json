{
  "Db": {
    "Host": "{{ .Values.db.serviceName }}",
    "Database": "{{ .Values.db.dbName }}",
    "Username": "{{ .Values.db.username }}",
    "Password": "{{ .Values.db.password }}"
  },
  "FileStorage": {
    "DirectoryPath": "{{ .Values.app.workStoragePath }}",
    "NetworkSharePath": "{{ .Values.app.networkSharePath }}"
  }
}
