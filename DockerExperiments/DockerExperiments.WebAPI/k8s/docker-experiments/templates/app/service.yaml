apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.id }}
  labels:
    app: {{ .Values.app.id }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.app.id }}
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http