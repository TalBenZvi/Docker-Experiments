apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.app.id }}
  labels: 
    app: {{ .Values.app.id }}
spec:
  to:
    kind: Service
    name: {{ .Values.app.id }}
  port:
    targetPort: http