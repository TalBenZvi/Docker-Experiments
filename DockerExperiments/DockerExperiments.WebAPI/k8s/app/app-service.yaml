apiVersion: v1
kind: Service
metadata:
  name: docker-example-webapi-service
spec:
  selector:
    app: docker-example-webapi
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort